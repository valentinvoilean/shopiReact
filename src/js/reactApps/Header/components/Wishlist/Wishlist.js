import React, { Component } from 'react';

export class Wishlist extends Component {
    static defaultProps = {
        name: 'Wishlist'
    };

    render() {
        return (
            <div> Wishlist </div>
        );
    }
}
